{
  "name": "bitget-journal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Bitget 只读 API 实盘交易日记（静态站 + 定时同步）",
  "scripts": {
    "sync": "node scripts/fetch.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "undici": "^6.21.0"
  }
}
